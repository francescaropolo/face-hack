<h2>Job detail</h2>
<div class="uk-grid-small uk-flex-wrap uk-flex-top uk-flex-row uk-flex-around@m uk-flex-around@l uk-flex-around@xl" uk-grid="masonry: true">
    <div class="uk-card uk-card-default uk-width-1-1@s uk-width-1-1@m uk-width-1-2@l uk-width-1-2@xl">
        <div class="uk-card-header">
            <div class="uk-grid-small uk-flex-middle" uk-grid>
                <div class="uk-width-expand">
                    <h3 class="uk-card-title uk-margin-remove-bottom">{{title}}</h3>
                    <p class="uk-text-meta uk-margin-remove-top">{{company}}</p>
                    <div class="uk-grid-small uk-flex-middle uk-flex-between" uk-grid>
                        <p class="uk-text-meta uk-margin-remove-top">{{type}}</p>
                        <p class="uk-text-meta uk-margin-remove-top">{{journeyType}}</p>
                        <p class="uk-text-meta uk-margin-remove-top">Vacancies: {{vacancies}}</p>
                    </div>
                </div>
            </div>
        </div>
        <div class="uk-card-body">
            <p>{{description}}</p>
        </div>
        <div class="uk-card-footer">
            <p>Salary: {{salary}}</p>
        </div>
        <div class="uk-card-footer">
            {{#if applicants}}
                <h3>Applicants</h3>
                <ul class="uk-list uk-list-divider">
                {{#each applicants}}
                    {{#if name}}
                    <li>
                        {{#if picturePath}}
                            <img src="{{picturePath}}" alt="profile_image" class="uk-border-circle" width="35" height="35">
                        {{/if}}
                        <a href="/users/{{this._id}}">{{name}} {{lastName}}</a>
                    </li>
                    {{else}}
                    <li>
                        {{#if picturePath}}
                            <img src="{{picturePath}}" alt="profile_image" class="uk-border-circle" width="35" height="35">
                        {{/if}}
                        <a href="/users/{{this._id}}">{{email}}</a>
                    </li>
                    {{/if}}
                {{/each}}
                </ul>
            {{/if}}
        </div>
    </div>
</div>